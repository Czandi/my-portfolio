import {
  trigger,
  state,
  style,
  transition,
  animate,
  query,
  group,
  animateChild,
  keyframes,
} from '@angular/animations';

const animateTime = '400ms cubic-bezier(.55, 0, .45, 1)';

// const popUp
