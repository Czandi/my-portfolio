export class Graphic {
  title: string;
  photoFileName: string;
  link: string;

  constructor(title: string, photoFileName: string, link: string) {
    this.title = title;
    this.photoFileName = photoFileName;
    this.link = link;
  }
}
